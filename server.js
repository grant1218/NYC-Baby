const express=require('express');const cors=require('cors');const Stripe=require('stripe');const app=express();app.use(cors());const stripe=process.env.STRIPE_SECRET_KEY?new Stripe(process.env.STRIPE_SECRET_KEY):null;const members=['grant','mikey','falconi','stuart','marney'];const state={signals:[],invites:[],paidMembers:[]};function normalize(x){return String(x||'').trim().toLowerCase()}function overlaps(){const by={};for(const s of state.signals){if(!s.item)continue;(by[s.item]??=[]).push(s)}return Object.entries(by).map(([item,arr])=>{const people=[...new Set(arr.map(x=>x.member))];if(people.length<2)return null;return{item,people,score:people.length,latest:arr.at(-1).createdAt}}).filter(Boolean).sort((a,b)=>b.score-a.score)}app.post('/api/billing/webhook',express.raw({type:'application/json'}),(req,res)=>{if(!stripe||!process.env.STRIPE_WEBHOOK_SECRET)return res.status(503).send('Billing not configured');let event;try{event=stripe.webhooks.constructEvent(req.body,req.headers['stripe-signature'],process.env.STRIPE_WEBHOOK_SECRET)}catch(e){return res.status(400).send('Invalid webhook signature')}const o=event.data.object;if(event.type==='checkout.session.completed'){state.paidMembers.push({email:normalize(o.customer_details&&o.customer_details.email),customer:o.customer,subscription:o.subscription,status:'paid',createdAt:new Date().toISOString()})}if(event.type==='customer.subscription.deleted'||event.type==='customer.subscription.updated'){const m=state.paidMembers.find(x=>x.subscription===o.id);if(m)m.status=o.status}res.json({received:true})});app.use(express.json());app.get('/health',(req,res)=>res.json({ok:true,service:'IN shared-signal API',billing:!!stripe}));app.get('/api/billing/config',(req,res)=>res.json({configured:!!(stripe&&process.env.STRIPE_PRICE_ID),amount:process.env.IN_MEMBERSHIP_AMOUNT?Number(process.env.IN_MEMBERSHIP_AMOUNT):null,interval:process.env.IN_MEMBERSHIP_INTERVAL||'month'}));app.post('/api/billing/checkout',async(req,res)=>{if(!stripe||!process.env.STRIPE_PRICE_ID)return res.status(503).json({error:'Membership checkout is not configured yet'});const email=normalize(req.body.email),name=String(req.body.name||'').trim();if(!email||!name)return res.status(400).json({error:'Name and email are required'});try{const site=process.env.IN_SITE_URL||'https://nyc-baby.onrender.com';const session=await stripe.checkout.sessions.create({mode:'subscription',line_items:[{price:process.env.STRIPE_PRICE_ID,quantity:1}],customer_email:email,success_url:site+'/join.html?success=1&session_id={CHECKOUT_SESSION_ID}',cancel_url:site+'/join.html?canceled=1',allow_promotion_codes:true,metadata:{member_name:name,source:'IN founding membership'},subscription_data:{metadata:{member_name:name,member_email:email}}});res.json({url:session.url})}catch(e){console.error('Stripe checkout error',e.message);res.status(500).json({error:'Could not start secure checkout'})}});app.get('/api/state',(req,res)=>{const member=normalize(req.query.member||'grant');res.json({member,members,signals:state.signals,overlaps:overlaps(),invites:state.invites.filter(x=>x.to===member||x.from===member)})});app.post('/api/signal',(req,res)=>{const member=normalize(req.body.member),item=normalize(req.body.item),value=String(req.body.value||'');if(!member||!item||!value)return res.status(400).json({error:'member, item, value required'});const rec={id:Date.now()+'-'+Math.random().toString(36).slice(2,7),member,item,value,createdAt:new Date().toISOString()};state.signals.push(rec);res.json({ok:true,signal:rec,overlaps:overlaps()})});app.post('/api/invite',(req,res)=>{const from=normalize(req.body.from),to=normalize(req.body.to),item=normalize(req.body.item);if(!from||!to||!item)return res.status(400).json({error:'from, to, item required'});const rec={id:Date.now()+'-'+Math.random().toString(36).slice(2,7),from,to,item,status:'open',createdAt:new Date().toISOString()};state.invites.push(rec);res.json({ok:true,invite:rec})});app.post('/api/reset',(req,res)=>{state.signals.length=0;state.invites.length=0;res.json({ok:true})});const port=process.env.PORT||3000;app.listen(port,()=>console.log('IN API listening on '+port));